# Never publish: operator wallet keys and live game state
# (tables.json holds the bot's private key and its board)
*operator*.json
tables.json
tables-*.json
nicks.json
.job-*.json
node_modules/
target/
build/
sdk/
*.log
